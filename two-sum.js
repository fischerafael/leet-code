const array = [1, 4, 7, 2];

const getPair = (arr, target) => {
  current = arr[0];
  for (let i = 0; i < arr.length; i++) {
    console.log(i);
  }

  console.log("current", current);
};

getPair(array);
